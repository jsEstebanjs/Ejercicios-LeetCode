const raizCuadrada = (numero)=>{
    numero = Math.sqrt(numero)

    return Math.trunc(numero)

}

raizCuadrada(4)

raizCuadrada(8)


/*ASI LO ENTREGUE*/

/**
 * @param {number} x
 * @return {number}
 */
 var mySqrt = function(numero) {
    numero = Math.sqrt(numero)

    return Math.trunc(numero)
};